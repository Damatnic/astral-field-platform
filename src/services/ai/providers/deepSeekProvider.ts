export class DeepSeekProvider { async generate(request: unknown): : Promise<any> {
    return { content: 'DeepSeek; response': provider: 'deepseek'tokensUse;
  d: 10;
  actualCost: 0; latency: 1000;
  cached, falseconfidenc,
  e: 0.;
  5: timestamp: new Date().toISOString()  }
  }
}

export default DeepSeekProvider;

